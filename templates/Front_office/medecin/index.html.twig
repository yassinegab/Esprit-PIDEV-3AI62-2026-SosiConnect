{% extends 'base.html.twig' %}

{% block title %}Dashboard Médecin{% endblock %}

{% block body %}
<<<<<<< HEAD
<div class="flex min-h-screen bg-gray-50">
    {% include 'components/sidebar.html.twig' %}

    <div class="flex flex-1 flex-col lg:ml-72">
        {% include 'components/top-nav.html.twig' %}

        <main class="flex-1 overflow-auto pb-12">
            <div class="mx-auto max-w-7xl space-y-8 p-4 lg:p-8">

                {# Header #}
                <div class="bg-gradient-to-r from-blue-600 to-purple-600 rounded-xl shadow-lg p-8 text-white">
                    <div class="flex items-center justify-between">
                        <div>
                            <h1 class="text-4xl font-bold mb-2">
                                Bienvenue Dr. {{ user.prenom }} {{ user.nom }}
                            </h1>
                            {% if user.specialite %}
                                <p class="text-blue-100 text-lg">
                                    <i class="fas fa-stethoscope mr-2"></i>
                                    {{ user.specialite }}
                                </p>
                            {% endif %}
                            <p class="text-blue-100 mt-2">
                                Gérez vos consultations et dossiers médicaux
                            </p>
                        </div>
                        <div class="hidden md:block">
                            <i class="fas fa-user-md text-white text-8xl opacity-20"></i>
                        </div>
                    </div>
                </div>

                {# Statistiques #}
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div class="bg-white border border-gray-200 rounded-xl p-6 hover:shadow-lg transition-shadow">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-lg font-semibold text-gray-900">Patients suivis</h3>
                            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                                <i class="fas fa-users text-blue-600 text-2xl"></i>
                            </div>
                        </div>
                        <p class="text-4xl font-bold text-gray-900">{{ totalPatients }}</p>
                        <p class="text-sm text-gray-500 mt-2">
                            <i class="fas fa-arrow-up text-green-500 mr-1"></i>
                            Patients enregistrés
                        </p>
                    </div>
                    
                    <div class="bg-white border border-gray-200 rounded-xl p-6 hover:shadow-lg transition-shadow">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-lg font-semibold text-gray-900">Consultations</h3>
                            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                                <i class="fas fa-calendar-check text-green-600 text-2xl"></i>
                            </div>
                        </div>
                        <p class="text-4xl font-bold text-gray-900">{{ nbRdvAujourdhui }}</p>
                        <p class="text-sm text-gray-500 mt-2">
                            <i class="fas fa-calendar text-blue-500 mr-1"></i>
                            Aujourd'hui
                        </p>
                    </div>
                    
                    <div class="bg-white border border-gray-200 rounded-xl p-6 hover:shadow-lg transition-shadow">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-lg font-semibold text-gray-900">Dossiers</h3>
                            <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center">
                                <i class="fas fa-folder-open text-purple-600 text-2xl"></i>
                            </div>
                        </div>
                        <p class="text-4xl font-bold text-gray-900">{{ totalDossiers }}</p>
                        <p class="text-sm text-gray-500 mt-2">
                            <i class="fas fa-file-medical text-purple-500 mr-1"></i>
                            Dossiers médicaux
                        </p>
                    </div>
                </div>

                {# Actions rapides #}
                <div class="bg-white rounded-xl border border-gray-200 shadow-sm p-6">
                    <h2 class="text-2xl font-bold text-gray-900 mb-6">
                        <i class="fas fa-bolt text-blue-600 mr-2"></i>
                        Actions rapides
                    </h2>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                        
                        <a href="{{ path('medecin_dossiers_liste') }}" 
                           class="group flex items-center p-5 border-2 border-gray-200 rounded-xl hover:border-blue-500 hover:bg-blue-50 transition-all">
                            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-blue-200 transition-colors">
                                <i class="fas fa-folder-open text-blue-600 text-2xl"></i>
                            </div>
                            <div>
                                <p class="font-bold text-gray-900 group-hover:text-blue-600">Consulter les dossiers</p>
                                <p class="text-sm text-gray-500">Accéder aux dossiers médicaux</p>
                            </div>
                        </a>
                        
                        <a href="{{ path('app_rendez_vous_index') }}" 
                           class="group flex items-center p-5 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all">
                            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-green-200 transition-colors">
                                <i class="fas fa-calendar-alt text-green-600 text-2xl"></i>
                            </div>
                            <div>
                                <p class="font-bold text-gray-900 group-hover:text-green-600">Voir les rendez-vous</p>
                                <p class="text-sm text-gray-500">Consultations planifiées</p>
                            </div>
                        </a>
                        
                        <a href="{{ path('medecin_profile') }}" 
                           class="group flex items-center p-5 border-2 border-gray-200 rounded-xl hover:border-purple-500 hover:bg-purple-50 transition-all">
                            <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mr-4 group-hover:bg-purple-200 transition-colors">
                                <i class="fas fa-user-edit text-purple-600 text-2xl"></i>
                            </div>
                            <div>
                                <p class="font-bold text-gray-900 group-hover:text-purple-600">Mon profil</p>
                                <p class="text-sm text-gray-500">Gérer mes informations</p>
                            </div>
                        </a>
                        
                    </div>
                </div>

                {# Derniers dossiers modifiés #}
                {% if derniersDossiers is not empty %}
                <div class="bg-white rounded-xl border border-gray-200 shadow-sm p-6">
                    <div class="flex items-center justify-between mb-6">
                        <h2 class="text-2xl font-bold text-gray-900">
                            <i class="fas fa-clock text-orange-600 mr-2"></i>
                            Derniers dossiers modifiés
                        </h2>
                        <a href="{{ path('medecin_dossiers_liste') }}" 
                           class="text-blue-600 hover:text-blue-700 font-medium">
                            Voir tout <i class="fas fa-arrow-right ml-1"></i>
                        </a>
                    </div>
                    
                    <div class="space-y-3">
                        {% for dossier in derniersDossiers %}
                        <a href="{{ path('medecin_dossier_detail', {'id': dossier.id}) }}"
                           class="flex items-center justify-between p-4 border border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                            <div class="flex items-center gap-4">
                                <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center">
                                    <i class="fas fa-user text-blue-600"></i>
                                </div>
                                <div>
                                    <p class="font-semibold text-gray-900">
                                        {{ dossier.user.prenom }} {{ dossier.user.nom }}
                                    </p>
                                    <p class="text-sm text-gray-500">
                                        Modifié le {{ dossier.derniereMiseAJour|date('d/m/Y à H:i') }}
                                    </p>
                                </div>
                            </div>
                            <i class="fas fa-chevron-right text-gray-400"></i>
                        </a>
                        {% endfor %}
                    </div>
                </div>
                {% endif %}

            </div>
        </main>
=======
<div class="min-h-screen bg-gray-50 p-6">
    <div class="max-w-7xl mx-auto">
        <div class="bg-white rounded-lg shadow-md p-8 mb-6">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
                Bienvenue Dr. {{ user.nom }}
            </h1>
            <p class="text-gray-600">
                Gérez vos consultations et dossiers médicaux
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
            <div class="bg-white border border-gray-200 rounded-lg p-6">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold">Patients</h3>
                    <i class="fas fa-users text-blue-600 text-2xl"></i>
                </div>
                <p class="text-3xl font-bold text-gray-900">0</p>
                <p class="text-sm text-gray-500 mt-2">Patients suivis</p>
            </div>
            
            <div class="bg-white border border-gray-200 rounded-lg p-6">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold">Consultations</h3>
                    <i class="fas fa-calendar-check text-green-600 text-2xl"></i>
                </div>
                <p class="text-3xl font-bold text-gray-900">0</p>
                <p class="text-sm text-gray-500 mt-2">Aujourd'hui</p>
            </div>
            
            <div class="bg-white border border-gray-200 rounded-lg p-6">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="text-lg font-semibold">Dossiers</h3>
                    <i class="fas fa-folder-open text-purple-600 text-2xl"></i>
                </div>
                <p class="text-3xl font-bold text-gray-900">0</p>
                <p class="text-sm text-gray-500 mt-2">À consulter</p>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6">
            <h2 class="text-xl font-semibold mb-4">Actions rapides</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <a href="{{ path('medecin_dossiers_liste') }}" 
                   class="flex items-center p-4 border border-gray-200 rounded-lg hover:bg-gray-50 transition">
                    <i class="fas fa-folder-open text-blue-600 text-xl mr-4"></i>
                    <div>
                        <p class="font-semibold">Consulter les dossiers</p>
                        <p class="text-sm text-gray-500">Accéder aux dossiers médicaux</p>
                    </div>
                </a>
                
                <a href="#" 
                   class="flex items-center p-4 border border-gray-200 rounded-lg hover:bg-gray-50 transition">
                    <i class="fas fa-calendar-plus text-green-600 text-xl mr-4"></i>
                    <div>
                        <p class="font-semibold">Planifier une consultation</p>
                        <p class="text-sm text-gray-500">Ajouter un rendez-vous</p>
                    </div>
                </a>
            </div>
        </div>
>>>>>>> origin/admin-userwellbeing-crud
    </div>
</div>
{% endblock %}