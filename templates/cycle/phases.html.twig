{% extends 'base.html.twig' %}

{% block body %}
<div class="flex min-h-screen bg-background">
  <div class="flex flex-1 flex-col">

    {# Top Navigation #}
    {% include 'components/top-nav.html.twig' %}


<div class="p-6">
    <h1 class="text-2xl font-bold text-center mb-6">Période fertile🌸</h1>
    <div id="calendar"></div>
</div>
 {# ---- Keymap / Légende ---- #}
        {# ---- Keymap / Légende ---- #}
       {# ---- Keymap / Légende ---- #}
<div class="flex justify-center items-center gap-6 mb-4">
    <div class="flex items-center gap-2">
        <span class="inline-block w-5 h-5 text-rose-500">🌸</span>
        <span class="text-gray-700 text-sm">Jour d'ovulation</span>
    </div>
    <div class="flex items-center gap-2">
        <span class="inline-block w-8 h-5 bg-green-100 border border-green-300 rounded-sm"></span>
        <span class="text-gray-700 text-sm">Période fertile</span>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/main.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
    const calendarEl = document.getElementById('calendar');

    const calendar = new FullCalendar.Calendar(calendarEl, {
        initialView: 'dayGridMonth',
        locale: 'fr',
        height: 650,
        events: '/phases/events',
        eventContent: function(arg) {
            // Afficher seulement emoji si défini, sinon vide
            let emoji = arg.event.extendedProps.emoji || '';
            return { html: emoji };
        }
    });

    calendar.render();
});
</script>
{% endblock %}
{% block stylesheets %}
<link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/main.min.css" rel="stylesheet">
<style>
    #calendar { max-width: 900px; margin: auto; }
    .fc-event {
        font-size: 1.2rem;
        display: flex;
        justify-content: center;
        align-items: center;
    }
</style>
{% endblock %} 